nextflow_process {

    name "Test Process MOSDEPTH"
    script "main.nf"
    process "MOSDEPTH"

    test("Should run mosdepth") {

        when {
            params {
            }
            process {
                """
                input[0] = ["SVP_test", file("${projectDir}/tests/data/SVP_test.bam.bai"), file("${projectDir}/tests/data/SVP_test.bam")] 
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
